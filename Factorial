#include <iostream>
using namespace std;

int main()
{
    int num = 0;
    int fat = 1;
    cout << "Enter a number: ";
    cin >> num;

    for (int i = num; i>0; i--)
    {
        fat= fat*i;
    }
    cout << "The factorial from number " << num << " is:"<<" "<< fat << endl;

    return 0;
}
